Below are a lecture's slides (SOURCE A) and the student's handwritten notes
(SOURCE B) for chapter {chapter}.

Identify every distinct concept the slides raise. A concept is a definition, a
theorem, an algorithm, a data structure, a property, or a technique. Do not
split one idea across two concepts, and do not merge two ideas into one.

For each concept record:
- name: the term as the slides write it, not as the textbook would
- slide_pages: every page it appears on
- slide_definition: how the slides define it, verbatim where possible, empty
  string if the slides never define it
- covered_by_notes: whether SOURCE B says anything substantive about it
- note_pages: the note pages that discuss it

SOURCE A:
{slides}

SOURCE B:
{notes}
